<p id="notice"><%= notice %></p>

<% provide(:title, @user.name) %>
<h1>
  <%= @user.name %> 's Profile
</h1>

<% if logged_in? %>
<%=link_to 'Logout'%>
<% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
